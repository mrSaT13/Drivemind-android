<?xml version="1.0" encoding="utf-8"?>
<!--
  По умолчанию весь трафик только HTTPS (usesCleartextTraffic=false).
  Исключение — только локальная разработка с LM Studio / Ollama на устройстве:
  localhost, 127.0.0.1 и 10.0.2.2 (эмулятор). Для реального устройства в LAN
  добавь свой IP, например 192.168.1.50, отдельным <domain>.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
